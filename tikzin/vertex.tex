%!TEX root = ../thesis.tex
%
\begin{tikzpicture}[baseline]
    \coordinate (i) at (0.0, -{sqrt(3)/4});
    \coordinate (x) at (0.5, +{sqrt(3)/4});
    \coordinate (o) at (1.0, -{sqrt(3)/4});
    %
    \node at (1.5, 0) {$=$};
    %
    \coordinate (0) at (2.5, 0);
    %
    \node at (3.5, 0) {$+$};
    %
    \coordinate (I) at (4.0, -{sqrt(3)/2});
    \coordinate (L) at (4.5, 0);
    \coordinate (X) at (5.0, +{sqrt(3)/2});
    \coordinate (R) at (5.5, 0);
    \coordinate (O) at (6.0, -{sqrt(3)/2});
    %
    \foreach \point in {i, 0, I}
        \draw [electron, gray] (\point) +(225:5mm) -- (\point);
    %
    \foreach \point in {x, 0, X}
        \draw [phonon, gray] (\point) -- +(90:5mm);
    %
    \foreach \point in {o, 0, O}
        \draw [electron, gray] (\point) -- +(-45:5mm);
    %
    \draw [phonon, double] (I) -- (O);
    \draw [electron, double] (I) -- (L);
    \draw [electron, double] (R) -- (O);
    %
    \draw [fill=black!10!white]
        (x) -- (i) -- (o) -- (x)
        (X) -- (L) -- (R) -- (X);
    %
    \foreach \point in {i, x, o, 0, I, L, X, R, O}
        \fill (\point) circle (1.5pt);
    %
    \foreach \point in {x, X}
        \node [below=3mm] at (\point) {$\Gamma$};
\end{tikzpicture}%
